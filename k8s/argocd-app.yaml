apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: oliveyoung-api
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://gitlab.com/clmakase_02/clmakase.git
    targetRevision: main
    path: k8s
    directory:
      recurse: true
      exclude: "version-c/**"
  destination:
    server: https://kubernetes.default.svc
    namespace: oliveyoung
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
